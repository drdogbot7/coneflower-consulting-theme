{% extends "views/base.twig" %}

{% block content %}

    {% for post in posts %}

        <div class="container my-8 prose">
            <div class="page-header">
                <h1>{{ post.title }}</h1>
            </div>
    
            {{ post.content }}
        </div>

    {% endfor %}

{% endblock %}